{
  "id": "E2E-001",
  "group": "E2E",
  "required_minimum": false,
  "description": "tools/list -> approve fingerprint -> tools/call -> digest -> WebAuthn -> forward -> log -> checkpoint",
  "object": null,
  "alg": "sha-256",
  "expected": {
    "status": "pending"
  },
  "note": "needs crypto backend: requires a real WebAuthn assertion and a signed checkpoint. Expected: all steps accept; the auditor verifies."
}
